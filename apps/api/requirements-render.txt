# MINIMAL REQUIREMENTS FOR RENDER DEPLOYMENT
# Compatible with Python 3.14 - NO RUST COMPILATION

# FastAPI - Use version compatible with Pydantic v1
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database (REQUIRED)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# Authentication (REQUIRED)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1

# Settings - Use Pydantic v1 (pure Python, no Rust)
pydantic==1.10.13
python-dotenv==1.0.0
email-validator==2.1.0.post1

# Excel Processing (REQUIRED for your app)
pandas
openpyxl

# Document Processing (REQUIRED for your app)
PyPDF2
python-docx

# Utilities (REQUIRED)
python-dateutil

# File Storage (REQUIRED)
supabase
storage3

# AI/ML (OPTIONAL - Can be disabled)
# openai

# OCR (DISABLED - Causes build issues)
# pytesseract
# pdf2image
# Pillow
# pdfplumber

# Auggie SDK (DISABLED - Compatibility issues)
# auggie-sdk

# Testing (NOT NEEDED in production)
# pytest
# pytest-asyncio
# httpx
